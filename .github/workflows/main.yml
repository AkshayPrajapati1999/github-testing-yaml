  name: "Build and Deploy"
  on:
    push:
      branches:
        - "build"
        - "test"
        - "deploy"
    pull_request:
      branches:
        - "master"
  jobs:
    build:
      runs-on: "ubuntu-latest"
      steps:
        - name: "echo test"
          uses: "actions/checkout@v2"
